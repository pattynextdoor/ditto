// TODO: You'll implement the Clap derive structs here
// This module defines the CLI interface for Ditto
//
// Needed structs:
// - Cli (top-level with global flags)
// - Commands enum (Init, Link, Unlink, Add, Status, Diff)
// - Each command's args struct
