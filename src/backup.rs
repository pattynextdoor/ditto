// TODO: You'll implement backup/restore logic here
//
// Needed functions:
// - backup(file: &Path, backup_dir: &Path) -> Result<()>
// - restore(file: &Path, backup_dir: &Path) -> Result<()>
// - has_backup(file: &Path, backup_dir: &Path) -> bool
// - backup_path_for(file: &Path, backup_dir: &Path) -> Result<PathBuf>
